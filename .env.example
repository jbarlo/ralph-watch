# Path to the directory containing ralph binaries (ralph, ralph-once)
# Required for running ralph commands from the UI
RALPH_BIN=/path/to/ralph/bin

# Directory to watch for ralph files (tickets.json, progress.txt)
# Defaults to current working directory if not set
RALPH_DIR=/path/to/project

# React editor integration (optional)
REACT_EDITOR=vim

# Terminal WebSocket server port (default: 3001)
TERMINAL_WS_PORT=3001

# Enable remote terminal access (DANGEROUS - only use on trusted networks like Tailscale)
# Set to this exact value to enable: yes-i-understand-this-is-dangerous
# RALPH_ENABLE_REMOTE_TERMINAL=yes-i-understand-this-is-dangerous
